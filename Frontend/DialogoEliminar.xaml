<Window x:Class="ProyectoRuben.Frontend.DialogoEliminar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Confirmar" Height="210" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

	<Border Background="White" CornerRadius="12" Margin="10">
		<Border.Effect>
			<DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.18" Direction="270"/>
		</Border.Effect>

		<Grid Margin="24">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<!-- Cabecera con icono -->
			<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
				<Border Width="36" Height="36" CornerRadius="18"
                        Background="#FFEBEE" Margin="0,0,12,0">
					<materialDesign:PackIcon Kind="AlertCircleOutline"
                                             Foreground="#D63031"
                                             Width="20" Height="20"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
				</Border>
				<TextBlock x:Name="TxtTitulo"
                           Text="Confirmar eliminación"
                           FontSize="16" FontWeight="SemiBold"
                           Foreground="#2D3436"
                           VerticalAlignment="Center"/>
			</StackPanel>

			<!-- Mensaje -->
			<TextBlock Grid.Row="1"
                       x:Name="TxtMensaje"
                       FontSize="14" Foreground="#636E72"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       Margin="0,0,0,4"/>

			<!-- Botones -->
			<StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,20,0,0">

				<Button x:Name="BtnCancelar"
                        Content="Cancelar"
                        Height="38" Padding="20,0"
                        Background="White"
                        Foreground="#636E72"
                        BorderThickness="1" BorderBrush="#DFE6E9"
                        Cursor="Hand" Margin="0,0,10,0"
                        Click="BtnCancelar_Click">
					<Button.Template>
						<ControlTemplate TargetType="Button">
							<Border CornerRadius="8"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
								<ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
							</Border>
						</ControlTemplate>
					</Button.Template>
				</Button>

				<Button x:Name="BtnEliminar"
                        Content="Eliminar"
                        Height="38" Padding="20,0"
                        Background="#D63031"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnEliminar_Click">
					<Button.Template>
						<ControlTemplate TargetType="Button">
							<Border CornerRadius="8"
                                    Background="{TemplateBinding Background}">
								<ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
							</Border>
						</ControlTemplate>
					</Button.Template>
				</Button>
			</StackPanel>
		</Grid>
	</Border>
</Window>
